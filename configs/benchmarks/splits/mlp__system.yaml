name: mlp
display_name: MLP (system split)
seed: 42
data:
  data_root: data
  task_catalog: data/task_catalog.csv
  stage3_authority_config: configs/v2/stage3/splits/system.yaml
  feature_cache: outputs/benchmarks/cache/features.sqlite3
features:
  kind: rdkit_2d
  radius: 2
  n_bits: 2048
model:
  hidden_dims:
  - 256
  - 128
  dropout: 0.1
training:
  optimizer: adamw
  learning_rate: 0.001
  weight_decay: 0.0001
  batch_size: 128
  max_epochs: 50
  loss: normalized_mse
  model_selection: final_training_state
  device: cuda
  precision: fp32
stage3:
  enabled: true
  tasks: all
  folds:
  - 1
  - 2
  - 3
  - 4
  - 5
