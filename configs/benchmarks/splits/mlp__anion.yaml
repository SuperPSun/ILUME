name: mlp
display_name: MLP (anion split)
seed: 42
data:
  data_root: data
  task_catalog: data/task_catalog.csv
  stage3_authority_config: configs/v2/stage3/splits/anion.yaml
  feature_cache: outputs/benchmarks/cache/features.sqlite3
features:
  kind: rdkit_2d
  radius: 2
  n_bits: 2048
model:
  hidden_dims:
  - 256
  - 128
  dropout: 0.1
training:
  optimizer: adamw
  learning_rate: 0.001
  weight_decay: 0.0001
  batch_size: 128
  max_epochs: 100
  early_stopping_patience: 20
  loss: normalized_mse
  selection_metric: raw_target_macro_mae
  device: cuda
  precision: fp32
stage3:
  enabled: true
  tasks: all
  folds:
  - 1
  - 2
  - 3
  - 4
  - 5
stage2_physics:
  enabled: false
  tasks: []
